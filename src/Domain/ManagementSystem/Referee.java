package Domain.ManagementSystem;

public class Referee extends EnrolledUser {
    public Referee(int userId, String userName, String password, String name) {
        super(userId, userName, password, name);
        //TODO: add other fields
    }
}
